// [PORT] Forwards to the copy under src/EclipseStudio/bin/. See that file for why this
// header exists at all; the short version is that Editors/LevelEditor.cpp and
// ObjectsCode/WORLD/tree.cpp spell the relative path with one fewer `../` than
// RENDERING/Deffered/RenderDeffered.cpp does, so it resolves to a second directory.

#pragma once

#include "../../../../../../bin/Data/Shaders/DX9_P1/system/LibSM/shadow_config.h"
